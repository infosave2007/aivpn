# AIVPN 威胁模型

本文档描述 AIVPN 的安全属性、系统所针对的攻击者模型，以及当前实现的已知局限性。

---

## 1. 安全目标

AIVPN 旨在提供：

1. **机密性** — 网络路径上的被动观察者无法获取载荷内容。
2. **流量分析抵抗** — 数据包大小、到达间隔和连接模式被伪装成已知应用程序的正常流量（WebRTC、QUIC 等）。
3. **主动探测抵抗** — 服务器不会对未经请求或格式错误的探测数据包作出可识别的响应。
4. **前向安全性** — 长期密钥的泄露不会导致之前捕获的会话被解密。
5. **抗审查可用性** — 系统能检测到伪装配置文件被 DPI 指纹识别的情况，并自动切换到新配置。

---

## 2. 攻击者模型

### 2.1 范围内的攻击者

| 攻击者 | 能力 | 威胁 |
|--------|------|------|
| **被动 ISP/审查机构** | 读取客户端与服务器之间的所有数据包 | 流量分析、协议识别 |
| **主动探测者** | 向服务器端点发送精心构造的数据包 | 通过服务器响应行为进行指纹识别 |
| **DPI 设备** | 对流量特征进行状态分析 | 伪装配置文件指纹识别、限速 |
| **网络观察者** | 跨多个观测点关联流量时序 | 流量关联分析 |
| **服务器密钥被窃** | 离线获取服务器静态私钥 | 无法解密过去的会话（PFS 保护） |
| **服务器被入侵** | 完全访问服务器内存和磁盘 | 当前会话暴露；历史会话受 PFS 保护 |

### 2.2 范围外的攻击者

| 攻击者 | 排除原因 |
|--------|---------|
| **客户端端点被入侵** | 若攻击者控制了运行客户端的操作系统或应用程序，任何 VPN 协议都无法提供保护。 |
| **全局被动攻击者** | 客户端 ISP 和服务器 ISP 之间的流量关联是一个难题；AIVPN 不声称能抵抗全局被动攻击者。 |
| **服务器物理没收** | 最好通过全盘加密和密钥销毁程序来处理，这超出了本协议的范围。 |
| **DNS 拦截** | 服务器主机名解析发生在 VPN 建立之前；请单独保护 DNS。 |

---

## 3. 密码学设计

### 3.1 密钥交换

- **算法：** X25519 Diffie-Hellman（临时客户端密钥对、静态服务器公钥）。
- **PSK：** 可选的 32 字节预共享密钥混入密钥派生过程，提供第二认证因素。
- **派生密钥：** 基于 BLAKE3 的 KDF 从 DH 输出派生 `session_key`、`tag_secret` 和 `nonce_suffix`。

### 3.2 认证加密

- **算法：** ChaCha20-Poly1305（IETF 变体，96 位 nonce）。
- **Nonce 构造：** `counter (8 字节) || nonce_suffix (4 字节)`。计数器在会话内单调递增；设计上防止重用。

### 3.3 会话标签（无会话 ID 的 O(1) 查找）

每个数据包携带一个 8 字节的*共振标签*，由当前时间戳和 `tag_secret` 派生。服务器为每个会话维护一个 256 条目的滑动窗口，以允许乱序传递。没有 `tag_secret` 的情况下，标签是不可猜测的。

**防重放：** 接受窗口外的标签（`DEFAULT_WINDOW_MS = 10 000 毫秒`）将被丢弃。XDP 早期过滤器在 NIC 层面执行相同的窗口检查。

### 3.4 完美前向安全（PFS 棘轮）

初始会话后，服务器发送携带新临时公钥的 `ServerHello`。客户端计算新的 DH 秘密，派生棘轮密钥，并立即开始使用新密钥发送数据。旧密钥在短暂保留以处理在途数据包后即被销毁。

**安全属性：** 攻击者即使录制了密文流并在事后获取了服务器静态私钥，也无法解密任何已完成至少一个棘轮步骤的会话。

---

## 4. 流量分析抵抗

### 4.1 伪装配置文件模拟

出站数据包按选定的流量配置文件（`MaskProfile`）进行整形：
- **头部注入：** 向每个数据包添加合成应用层头部。
- **大小整形：** 对载荷进行填充以匹配目标大小分布。
- **时序整形（IAT）：** 模拟引擎执行配置文件中的到达间隔分布。
- **FSM 状态转换：** 流量通过有限状态机演进，匹配目标应用程序的对话阶段。

### 4.2 神经共振（自动配置文件轮换）

每个配置文件对应一个神经网络（约 66 KB，由配置文件签名向量确定性派生），实时监控流量统计数据。当重建误差（MSE）超过 `compromised_threshold = 0.50` 时，服务器触发配置文件轮换，并将新配置推送给已连接的客户端。

监控特征：数据包大小分布、IAT 统计、熵值、突发模式、数据包方向比、IAT 周期性。

**轮换冷却时间：** 两次轮换之间 60 秒的冷却时间，防止在持续主动探测下出现振荡。

### 4.3 主动探测抵抗

- 服务器不响应标签验证失败的数据包。
- 标签验证需要知道 `tag_secret`，而后者只能在成功完成 DH 握手后才能派生。
- 未经请求的探测不会收到响应，使服务器协议对外部观察者而言与 UDP echo 服务或游戏服务器无法区分。

---

## 5. 机器学习组件

AIVPN 内置两个体积很小、完全自包含的 ML 组件，用于保护第 4 节所述的伪装配置文件。两者都不是 LLM，也都不需要 GPU——都以常量权重的形式烘焙进二进制文件，运行耗时以微秒计。它们分别回答两个不同的问题：

| 组件 | 回答的问题 | 所属类别 | 位置 |
|---|---|---|---|
| **Neural Resonance（神经共振）** | “*实时*流量是否仍然符合这个伪装配置文件的指纹？” | 全连接 MLP **自编码器**（异常检测） | `crates/aivpn-server/src/neural.rs` |
| **ML-DPI 门控** | “被伪装的流量*读起来是不是像隧道*，而不是像目标协议？” | **GBDT**（梯度提升决策树） | `crates/aivpn-server/src/dpi_gate.rs` *（R2 Phase D）* |

两者最终都指向同一个动作：当伪装配置文件看起来已被识破时，**轮换配置文件**。

### 5.1 Neural Resonance——重建型自编码器

#### 网络类型

一个**前馈全连接多层感知机（MLP）**，用作**自编码器（autoencoder）**——*不是*卷积网络，*不是*循环网络，也*不是* Transformer。

```
input (64) ──▶ Linear(64→128) ──▶ ReLU ──▶ Linear(128→64) ──▶ output (64)
                                                                    │
              reconstruction error  MSE(input, output)  ◀──────────┘
```

- **输入：** 一个 64 维特征向量，汇总会话近期流量——数据包大小直方图（16 个区间，从 0 到 ≥1280 字节）、字节熵、每秒数据包数、每秒字节数，以及到达间隔时间统计——通过饱和变换（`saturate()`）归一化，防止长尾数据把量纲拉爆。
- **输出：** 同一向量的 64 维*重建结果*。
- **评分：** `MSE(input, output)`——均方重建误差，也就是**共振分数（resonance score）**。

由于输入和输出共享相同的 64 个维度，且模型经过训练（此处为*烘焙*）以重现该伪装配置文件的“正常”指纹，它的行为如同一个经典的**无监督新颖性检测器（unsupervised novelty detector）**：

- **MSE 较低** → 实时流量与该配置文件匹配 → 健康。
- **MSE 较高** → 实时流量已偏离该配置文件的特征签名 → 该配置文件很可能已被 DPI 识别 → **轮换**。

#### 特别之处：权重是*派生*出来的，而不是*训练*出来的

一个与众不同的设计选择：这个 MLP 的权重**并非通过反向传播学习得到**。它们是从伪装配置文件自身的 64 维浮点 `signature_vector`（签名向量）中**确定性地烘焙**出来的——每个权重都由该签名的 BLAKE3 哈希播种而来（`MaskNet::from_signature`）。由此带来的结果：

- 每个伪装配置文件都会产生**属于自己**的网络——相当于每个配置文件都有一个专属的“共振腔”。
- 没有训练数据、没有梯度下降、也没有模型文件：这个网络是配置文件的纯函数，因此客户端和服务器能够派生出完全相同的网络。
- 从概念上讲，这更接近**随机投影 / 极限学习机（Extreme Learning Machine, ELM）风格**的固定权重自编码器，而不是传统意义上训练出来的模型。重建之所以有效，是因为固定投影*正是针对该配置文件的指纹调校的*：符合指纹的输入能够很好地重建，不符合的则不能。

#### 阈值与轮换逻辑

- 默认值（`NeuralConfig`）：`warning_threshold = 0.35`，`compromised_threshold = 0.50`，每 `30 秒` 检查一次，轮换冷却时间 `60 秒`。这些默认值是针对 realcap2 真实抓包语料库、在每个内置伪装配置文件上重放校准得到的（健康流量 MSE：总体 p99 ≈ 0.26，最大值 ≈ 0.26；在实测环境中观测到最高约 0.31）——详见 `crates/aivpn-server/examples/neural_calib.rs`。
- 这些固定默认值仅在**预热窗口**内生效。达到 `MIN_CALIBRATION_SAMPLES` 之后，系统会切换到**逐配置文件的自适应校准**：阈值变为该配置文件自身观测到的 MSE 分布的 `mean + 1.5σ`（警告）和 `mean + 3σ`（已被攻破）——也就是说，每个配置文件都依据自身的基线来评估，而不是依据一个全局常量。
- 超过 `compromised_threshold`（受冷却时间约束）会在网关中触发**伪装配置文件轮换**。

> 专利申请中。其核心价值在于：这是一种 O(1)、无状态、按配置文件独立进行的异常检测，不需要任何带标签的攻击数据——它检测的是*与自身的偏离*，因此能够应对它从未见过的新型 DPI 指纹识别技术。

### 5.2 ML-DPI 门控——GBDT “读起来像隧道”分类器（R2 Phase D）

#### 模型类型

一个**梯度提升决策树集成模型（GBDT，gradient-boosted decision trees）**——由多棵浅层决策树组成的集成模型，其加性投票结果给出一个概率值。**完全不是神经网络**：这是另一个 ML 类别，之所以选它，是因为它成本低、不需要矩阵运算，并且能很好地处理大小/熵/头部等混合特征。

#### 它做什么

完整的深度包检测（运行 nDPI）对于实时数据路径而言过于昂贵。GBDT 是对 DPI 判定结果的**廉价内联近似**：给定一个会话的一个数据包窗口，它输出该流量**读起来像被混淆的隧道**（而不是像其声称的目标协议）的概率。

- **特征（共 23 个，全部廉价，不做深度载荷解析）：** 数据包大小的矩与直方图、前 16 字节的熵，以及若干协议结构性检查（STUN message-type / magic-cookie / 长度一致性、QUIC long-header 格式）。到达间隔时间被特意**排除**——在离线研究中，它被证明存在信息泄漏且不稳定。
- **训练**（离线，`research/mask-generation/r2/`）：采用“教师—学生”方案——由 nDPI 对合成流量和真实抓包进行标注，GBDT 学习复现该判定结果。采用严格的**分组**交叉验证（按伪装配置文件切分，避免数据泄漏）：在伪装域上达到 **93.3%** 的准确率，而二分类的“读起来像隧道”门控达到了 **precision + recall = 1.000**——它从未误判一个真正的伪装流量，同时抓住了每一个失效的流量。
- **部署：** 训练好的树集成模型被导出为**嵌入服务器二进制文件的常量权重**（与烘焙 MLP 的思路相同），就地遍历——没有模型文件，热路径上没有内存分配。由 Cargo 特性 `neural` 控制启停。

#### 与 Neural Resonance 如何协同

GBDT 的判定结果是一个**伴生信号（sibling signal）**，而非替代品。两者共同驱动同一个网关轮换决策：

- **Neural Resonance** 监测*偏离自身的漂移*（这个伪装配置文件不再像它自己了）。
- **ML-DPI 门控** 监测*“读起来像隧道”*的迹象（这个伪装配置文件在类 DPI 分类器看来像是混淆流量）。

两者中任意一个越过阈值 ⇒ 该伪装配置文件即被视为已被攻破 ⇒ 触发轮换。两个来自不同类别、彼此独立的检测器让触发机制更加稳健：能骗过其中一个的 DPI 手段，未必能同时骗过另一个。

### 5.3 相关的离线 ML（不在数据路径上）

同一个 GBDT / 判别器也为**离线**工具链提供支持，因此在那里完全不产生运行时延迟：

- **CI DPI 门控**（R2 Phase A）：每个待发布的伪装配置文件在合并前都会先经过 nDPI 分类。
- **对抗式配置文件修复**（R2 Phase C，`aivpn-mask-repair`）：未通过 DPI 门控的配置文件会通过一个有界的爬山搜索自动*修复*，该搜索由真实的 nDPI 判别器打分，直到该配置文件被分类为其目标协议为止。

设计细节参见：`docs/R2_DESIGN.md`、`docs/R2_PHASE_{A,C,D}.md`。

---

## 6. Kill-Switch 与防泄漏保护

当 `--kill-switch` 处于活动状态时，客户端安装防火墙规则，阻止所有出站流量，仅允许：
- 通过 VPN TUN 接口的流量。
- 到物理 VPN 服务器 IP 的流量（以便重新建立隧道）。
- 回环流量。

**实现方式：**
- **Linux：** 带 drop 策略的 nftables 表 `aivpn_ks`；iptables 链 `AIVPN_KS` 作为备用。
- **macOS：** pfctl 锚点 `aivpn_ks`。
- **Windows：** 通过 `netsh advfirewall` 配置 Windows 防火墙规则。

规则在进程意外终止（SIGKILL）后有意保持有效——用户持续受到保护，直到显式运行 `aivpn-client kill-switch clear`。

**无命令注入：** 所有防火墙规则参数作为独立的 `argv` 元素传递；不通过 shell 进行字符串插值。

**macOS 安全写入：** pfctl 锚点规则写入 `/var/run/aivpn/`（模式 `0700`），使用 `O_NOFOLLOW | O_CREAT_NEW | mode(0o600)` 防止针对全局可写目录的符号链接攻击。

---

## 7. XDP 早期数据包过滤

当安装了 `xdp_prog.o` 时，客户端将 XDP BPF 程序附加到物理 NIC（默认路由接口）。过滤器在 NIC RX 层面运行，在套接字缓冲区分配之前：

- 丢弃短于 26 字节的 UDP 数据包（AIVPN 最小有效载荷）。
- 丢弃共振标签时间戳超出接受窗口（默认距 `bpf_ktime_get_ns()` ±10 秒）的 UDP 数据包。
- 所有其他数据包不经修改直接传递到正常网络栈。

**效果：** 具有随机载荷的大流量 DDoS 数据包在消耗内核网络资源之前就被丢弃。合法流量不受影响。

**故障模式：** 如果 `xdp_prog.o` 不存在或附加失败，VPN 照常运行而不使用 XDP——这是尽力而为的优化，而非安全要求。

---

## 8. 已知局限性

| 局限性 | 说明 |
|--------|------|
| **XDP 过滤器仅支持 IPv4** | IPv6 数据包无条件通过 XDP。对于纯 IPv6 部署，这会降低 NIC 层面的 DDoS 防护能力。 |
| **流量关联** | 客户端 ISP 和服务器 ISP 之间的时序关联可能被资源充足的攻击者利用。 |
| **伪装配置文件质量** | 制作粗糙的配置文件（置信度分数低）可能被训练有素的分类器与真实流量区分开来。部署自定义配置文件前请使用 `--validate-mask` 验证。 |
| **单跳架构** | AIVPN 是单跳 VPN；服务器知道客户端的真实 IP。如需匿名性，请结合可信出口节点使用。 |
| **PSK 分发** | 嵌入连接密钥中的 PSK 必须安全分发。连接密钥字符串泄露将允许攻击者冒充客户端。 |
| **Kill-switch 仅持续到重启** | 防火墙规则加载在运行中的内核/防火墙中；重启后不保留。这是有意为之——重启后系统以干净状态启动。 |

